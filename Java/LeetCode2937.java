package Java;

public class LeetCode2937 {
    public int findMinimumOperations(String s1, String s2, String s3) {
        int len1=s1.length();
        int len2=s2.length();
        int len3=s3.length();
        for(int i = 0; i<Math.min(len1,len2,len3); i++){
            
        }
    }
}
